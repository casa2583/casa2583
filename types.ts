
export interface Achievement {
  title: string;
  description: string;
  icon: string;
}

export interface Program {
  title: string;
  description: string;
  tags: string[];
  imageUrl: string;
}

export interface PartInfo {
  name: string;
  description: string;
}
